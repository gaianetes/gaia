{{ if .Values.global.sonarr.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr
  labels:
    {{- include "mediaserver.labels" . | nindent 4 }}
  {{- with .Values.global.sonarr.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.global.sonarr.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.global.general.ingress_host | quote }}
      secretName: {{ .Values.global.sonarr.ingress.tls.secretName }}
{{ end }}
  ingressClassName: {{ .Values.global.general.ingress.ingressClassName }}
  rules:
    - host: {{ .Values.global.general.ingress_host | quote }}
      http:
        paths:
          - path: {{ .Values.global.sonarr.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  number: {{ .Values.global.sonarr.service.port }}
{{ end }}