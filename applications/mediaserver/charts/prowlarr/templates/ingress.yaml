{{ if .Values.global.prowlarr.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prowlarr
  labels:
    {{- include "mediaserver.labels" . | nindent 4 }}
  {{- with .Values.global.prowlarr.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{ if .Values.global.prowlarr.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.global.general.ingress_host | quote }}
      secretName: {{ .Values.global.prowlarr.ingress.tls.secretName }}
{{ end }}
  ingressClassName: {{ .Values.global.general.ingress.ingressClassName }}
  rules:
    - host: {{ .Values.global.general.ingress_host | quote }}
      http:
        paths:
          - path: {{ .Values.global.prowlarr.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: prowlarr
                port:
                  number: {{ .Values.global.prowlarr.service.port }}
{{ end }}